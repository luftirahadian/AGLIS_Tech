# Environment Configuration
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# Database Configuration
DB_USER=isp_admin
DB_HOST=localhost
DB_NAME=isp_management
DB_PASSWORD=isp_secure_password_2024
DB_PORT=5432

# JWT Configuration
JWT_SECRET=isp_jwt_secret_key_2024_very_secure_and_long
JWT_EXPIRES_IN=7d

# CORS Configuration
# For development: leave empty to auto-detect local network
# For production: specify your domain(s), comma-separated
# Examples:
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
# CORS_ORIGIN=https://api.yourdomain.com
CORS_ORIGIN=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=10000

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR=uploads

# Socket.IO Configuration
SOCKET_ORIGIN=
